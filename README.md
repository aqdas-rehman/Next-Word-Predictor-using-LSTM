# Next-Word-Predictor-using-LSTM
Next-word prediction with an LSTM network in TensorFlow/Keras. Tokenizes text, builds n-gram sequences, and trains an Embedding + LSTM model to suggest the next word, then generates whole sentences by predicting one word at a time. Includes the full NLP pipeline from tokenization to text generation.
